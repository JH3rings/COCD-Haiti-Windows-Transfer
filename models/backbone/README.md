# Backbone

The final Teacher and Student share `ConvNeXtTinyFPN` from
`cocd/models/landslide_cocd.py`.  It consumes the five-channel target-orbit
pre/post SAR view documented in `dataset/README.md`.
