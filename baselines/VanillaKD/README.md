# Vanilla KD

The VKD configuration is the `VKD` arm of `cocd/scripts/23_train_ours_v2.py`:
R1 target-only Student with full-pixel ordinary logit KD.  The recovered run is
exported by `cocd/windows_main/retest_vanilla_kd.py`; it is test-monitored
development evidence.
