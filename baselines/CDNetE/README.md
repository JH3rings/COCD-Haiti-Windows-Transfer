# CDNetE Early Fusion

Source runner: `cocd/scripts/23_train_external_baselines.py` (`cdnette`).
Input is the target-only 5-channel stack.  Metrics and visual outputs are in
the complete paper package.
